Pod::Spec.new do |s|
  s.name         = 'BitFiddle'
  s.version      = '0.0.1'
  s.summary      = 'Directly manipulate data with expressive and type-safe interfaces.'
  s.description  = <<-TODO
  s.license      = 'MIT'
  s.author    = 'Ryan Wachowski'
  s.source       = { git: 'http://EXAMPLE/BitFiddle.git', tag: "#{s.version}" }
  s.source_files  =  "BitFiddle/Sources/**/*.{h,m,swift}"
end
